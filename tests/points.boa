(fun (mkpt x y)
    (vek x y)
)
(fun (pointsum pt1 pt2)
    (vek (+ (index pt1 0) (index pt2 0)) (+ (index pt1 1) (index pt2 1)))
)

(if (= input 1)
    (pointsum (mkpt 1 2) (mkpt 2 3))
    (if (= input 2)
        (pointsum (pointsum (mkpt 1 1) (mkpt 1 1)) (pointsum (mkpt 1 1) (mkpt 1 1)))
        (if (= input 3)
            (pointsum (pointsum (print (mkpt 1 1)) (mkpt 1 1)) (print (pointsum (mkpt 1 1) (mkpt 1 1))))
            (vek (pointsum (mkpt 1 1) (mkpt 1 1)) (pointsum (mkpt 2 2) (mkpt 2 2)))
        )
    )
)
